import * as React from 'react';
import { Text } from 'react-native';
import Stack from 'react-native-stack-view';

export default function App() {

  return (
    <Stack color={'red'} padding={50}>
      <Text>Hello</Text>
    </Stack>
  );
}
